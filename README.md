# pulmonary_embolism_detection
This repo contains model for pulmonary embolism detection, classifation and segmentation (unified approach). It contains code for 2 models- 
1) Trained on only 2500 images (datapoint)- subset dataset
2) Trained on wjole dataset for pulomary embolism available on kagle competition.
